<?xml version="1.0" encoding="UTF-8"?>
<Global genome="hg19" locus="{{ position.contig }}:{{ position.start }}-{{ position.stop }}" version="3">
	<Resources>
		{% if vcf_path %}
		<Resource name="VCF" path="{{ url_for('pileup.remote_static', file=vcf_file, _external=True) }}"/>
		{% endif %}
		{% for alignment in alignments %}
		<Resource name="{{ alignment.sample }}" path="{{ url_for('pileup.remote_static', file=alignment.bam, _external=True) }}"/>
		{% endfor %}
	</Resources>
</Global>
