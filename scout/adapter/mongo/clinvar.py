# -*- coding: utf-8 -*-
import logging

import pymongo

log = logging.getLogger(__name__)

class ClinVarHandler(object):

    def add_submission(self, submission_obj, user_obj, institute_obj, case_obj)
